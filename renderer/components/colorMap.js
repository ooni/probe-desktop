import {
  theme
} from 'ooni-components'

const colorMap = {
  anomaly: theme.colors.yellow9,
  blocked: theme.colors.red7,
  reachable: theme.colors.green8,
  error: theme.colors.gray6
}

export default colorMap
